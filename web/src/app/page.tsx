import { redirect } from 'next/navigation';

export default function Home() {
    // return <div>첫 페이지</div>;

    // redirect('/solplace-logs/new');
    // redirect('/mypage');
    // redirect('/solplace-logs/');
    // redirect('/signup');
    // redirect('/login');
    redirect('/splash');
}
